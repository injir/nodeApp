extends ../layouts/navigation
block content
		section
		div(data-uk-grid).container
			each val in model
				article.item.uk-width-1-3
					a(href='/models/'+val._id).image.fit
						img(src='/3d/'+val._id+'/image/'+val.img)
					header
						h3=val.title
						p=val.author
				